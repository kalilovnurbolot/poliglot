# Copy to backend/.env.ip on the server and fill in real values.
# These values are literal (not auto-filled from the root .env.ip) — write the
# actual server IP, not ${SERVER_IP}.

SECRET_KEY=generate-a-long-random-value-here
DEBUG=False
ALLOWED_HOSTS=45.130.166.187
CORS_ALLOWED_ORIGINS=http://45.130.166.187:3000

GOOGLE_OAUTH_CLIENT_ID=your-client-id.apps.googleusercontent.com
