# Read by docker-compose itself (not by the containers) to fill in ${...} placeholders
# in docker-compose.yml — currently just the frontend's build-time Google client_id.
GOOGLE_OAUTH_CLIENT_ID=your-client-id.apps.googleusercontent.com
